name: Test
on:
  push:
    branches: [main]
    paths-ignore:
      - "mat_to_squirrel/**"
  pull_request:
    branches: [main]
    paths-ignore:
      - "mat_to_squirrel/**"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required"'
